---
export interface Props {
	startDate: string;
	finishDate: string;
}
const { startDate, finishDate } = Astro.props;
---

<div class="reading-time">
	{ startDate  && <div><h4>Started:</h4><p>{startDate}</p></div> }
	{ finishDate && <div><h4>Finished:</h4><p>{finishDate}</p></div> }
</div>
